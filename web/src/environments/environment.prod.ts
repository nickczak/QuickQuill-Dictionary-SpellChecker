// Production environment: the Angular app is served from Vercel at
// https://quickquill.ink. The browser calls the backend directly at its own
// custom domain because the frontend host does not proxy /api.
export const environment = {
  apiBaseUrl: 'https://api.quickquill.ink',
};
