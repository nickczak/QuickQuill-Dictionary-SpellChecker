// Development environment: the app is served on the same origin as the API
// (Angular dev server proxies /api to the backend), so no base URL prefix.
export const environment = {
  apiBaseUrl: '',
};
